name: paws_toilet_aire
version: 0.1.0+1
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.17
  cloud_firestore: ^5.6.0
  firebase_core: ^3.10.0
  firebase_storage: ^12.4.0
  geolocator: ^13.0.2
  http: ^1.2.2
  image_picker: ^1.1.2
  intl: ^0.20.1
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/paws_logo.svg
    - assets/images/paws_final_badge.svg
